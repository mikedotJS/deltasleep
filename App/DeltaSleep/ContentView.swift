import SwiftUI

/// Placeholder root view. The real main screen (§5, P7) replaces this
/// once P3 (snapshot cache) and P5 (shared components) land.
struct ContentView: View {
    var body: some View {
        Text("deltasleep")
    }
}

#Preview {
    ContentView()
}
