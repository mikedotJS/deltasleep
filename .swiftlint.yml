included:
  - App
  - Widget
  - Packages

excluded:
  - Packages/*/.build
  - "**/.build"

disabled_rules:
  - todo

opt_in_rules:
  - empty_count
  - closure_spacing
  - explicit_init

line_length:
  warning: 120
  error: 160

identifier_name:
  min_length: 2
