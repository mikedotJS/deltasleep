/// The sleep-debt domain model and weighted debt engine.
///
/// Pure Swift and Foundation only — no HealthKit, no SwiftUI, no WidgetKit.
/// This is what lets the engine be unit tested without a simulator and
/// reused unmodified by both the app and the widget extension.
///
/// Implementation lands in P1 (docs/IMPLEMENTATION_PLAN.md §1.1, §5).
/// This file is a placeholder so the package has something to build and
/// test against from P0 onward.
public enum SleepDebtCore {
    public static let packageName = "SleepDebtCore"
}
